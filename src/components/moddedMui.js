
import { styled } from '@mui/system';
import Snackbar from '@mui/material/Snackbar'

export const ModdedSnackbar = styled(Snackbar)({
    "& div": {
        width: "100%",
        textAlign: "center"
    },
});

export default function ModdedMui() {
    return (
        <></>
    )
}
